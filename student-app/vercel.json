{
  "builds": [
    {
      "src": "student-app/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "student-app/$1" }
  ]
}
